<%= form_with(model: isotherm, local: true) do |form| %>
  <% if isotherm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(isotherm.errors.count, "error") %> prohibited this isotherm from being saved:</h2>

      <ul>
      <% isotherm.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
